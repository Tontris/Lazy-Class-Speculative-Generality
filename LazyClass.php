<?php
class LogService {
    public function logMessage($message) {
        // Цей метод повинен записувати повідомлення в журнал, але наразі він порожній
    }

    public function logError(Exception $error) {
        // Цей метод повинен записувати помилки в журнал, але наразі він порожній
    }
}